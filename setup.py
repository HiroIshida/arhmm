import sys
from setuptools import setup, find_packages

setup_requires = []

install_requires = [
        "numpy",
        ]

setup(
    name='arhmm',
    version='0.0.1',
    description='',
    license='MIT',
    install_requires=install_requires,
)
